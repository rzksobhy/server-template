# Server Template

This is a template for backend projects for `PERN` (Postgres Express React Node.js) stack projects

## It uses
- `node-pg-migrate` to handle updates and migrations on the database
- `Swagger` to document you APIs using comments on the route definition (in the `src/routes` folder)